#!/bin/bash

# Claude API Quick Switch Tool
# A concise API provider switching tool designed for developers
# Supports: GLM, Kimi2, MiniMax, etc.

# Version: 2.3.0
# Multi-language Support Enhanced
# Features: Configuration validation, health check, and router integration

# Load multi-language system
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/i18n/loader.sh"

# Color definitions
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
NC='\033[0m' # No Color

# Configuration paths
CONFIG_DIR="$HOME/.claude/configs"
CLAUDE_CONFIG="$HOME/.claude/settings.json"
BACKUP_DIR="$HOME/.claude/backups"
ACTIVE_MARKER="$HOME/.claude/.active_config"

# 备用配置路径（用于权限问题时的fallback）
ALTERNATIVE_CLAUDE_CONFIG="$HOME/.claude-config/settings.json"
ALTERNATIVE_CONFIG_DIR="$HOME/.claude-config/configs"

# 确保目录存在
ensure_dirs() {
    mkdir -p "$CONFIG_DIR" "$BACKUP_DIR"
}

# 检查配置文件
check_config_file() {
    if [ ! -f "$CLAUDE_CONFIG" ]; then
        echo -e "${RED}$(format_text "messages.errors.config_not_found" "path" "$CLAUDE_CONFIG")${NC}"
        echo -e "${YELLOW}💡 $(get_text "messages.info.create_config_first")${NC}"
        return 1
    fi
}

# 验证JSON格式
validate_json() {
    local file="$1"
    if command -v python3 >/dev/null 2>&1; then
        python3 -c "import json; json.load(open('$file'))" 2>/dev/null
    elif command -v jq >/dev/null 2>&1; then
        jq empty "$file" 2>/dev/null
    else
        # 简单JSON格式检查
        grep -q '^{.*}$' "$file" && grep -q '.*}$' "$file"
    fi
}

# 验证配置完整性
validate_config_completeness() {
    local config_file="$1"

    if ! command -v jq >/dev/null 2>&1; then
        echo "⚠️ 需要安装jq以进行完整验证"
        return 0
    fi

    local warnings=()
    local errors=()

    # 检查必要字段
    local auth_token=$(jq -r '.env.ANTHROPIC_AUTH_TOKEN // ""' "$config_file")
    local base_url=$(jq -r '.env.ANTHROPIC_BASE_URL // ""' "$config_file")
    local model=$(jq -r '.env.ANTHROPIC_DEFAULT_SONNET_MODEL // ""' "$config_file")
    local provider=$(jq -r '.provider // ""' "$config_file")

    # 语言界面配置特殊处理：跳过API密钥验证
    if [ "$provider" = "language-ui" ]; then
        echo -e "${GREEN}$(get_text "messages.success.language_valid")${NC}"
        echo -e "${CYAN}$(get_text "messages.success.language_info")${NC}"
        return 0
    fi

    # 检查API密钥（仅对API提供商配置）
    if [ -z "$auth_token" ] || [ "$auth_token" = "your-api-key-here" ] || [ "$auth_token" = "sk-your-api-key-here" ]; then
        errors+=("❌ $(get_text "messages.errors.api_key_placeholder")")
    fi

    # 检查API端点
    if [ -z "$base_url" ]; then
        errors+=("❌ API端点未配置")
    elif [[ "$base_url" != "https://api.anthropic.com" ]]; then
        # 验证第三方API端点格式
        if [[ ! "$base_url" =~ ^https://[^/]+/anthropic/?$ ]]; then
            warnings+=("⚠️ API端点格式可能不正确，建议以 /anthropic 结尾")
        fi
    fi

    # 检查模型名称
    if [ -z "$model" ] || [ "$model" = "unknown" ]; then
        errors+=("❌ 模型名称未配置或无效")
    fi

    # 显示验证结果
    if [ ${#errors[@]} -gt 0 ]; then
        echo -e "${RED}$(get_text "messages.errors.config_validation_failed")${NC}"
        for error in "${errors[@]}"; do
            echo "  $error"
        done
        return 1
    fi

    if [ ${#warnings[@]} -gt 0 ]; then
        echo -e "${YELLOW}$(get_text "messages.warnings.config_warnings")${NC}"
        for warning in "${warnings[@]}"; do
            echo "  $warning"
        done
    fi

    return 0
}

# 验证API连接性（可选）
validate_api_connectivity() {
    local base_url="$1"

    if command -v curl >/dev/null 2>&1; then
        echo "🔍 检查API连接性..."
        if curl -s --connect-timeout 5 "$base_url" >/dev/null 2>&1; then
            echo "✅ API端点可达"
            return 0
        else
            echo "⚠️ API端点不可达，请检查网络连接"
            return 1
        fi
    else
        echo "📝 需要安装curl以检查API连接性"
        return 0
    fi
}

# 验证所有配置文件
validate_all_configs() {
    echo -e "${BLUE}$(get_text "validate.title")${NC}"
    echo "=================="
    echo ""

    local total=0
    local valid=0
    local invalid=0
    local warnings=0

    # 遍历所有配置文件
    for config in "$CONFIG_DIR"/*.json; do
        if [ -f "$config" ]; then
            ((total++))
            local name=$(basename "$config" .json)

            # 跳过语言界面配置
            case "$name" in
                zh-ui|en-ui|test-*|*.tmp|*.backup*)
                    continue
                    ;;
            esac

            echo -n "检查 $name... "

            # JSON格式验证
            if ! validate_json "$config"; then
                echo -e "${RED}❌ JSON格式错误${NC}"
                ((invalid++))
                continue
            fi

            # 配置完整性验证
            if validate_config_completeness "$config" >/dev/null 2>&1; then
                echo -e "${GREEN}✅ 通过${NC}"
                ((valid++))
            else
                echo -e "${YELLOW}⚠️ 有警告${NC}"
                ((warnings++))
            fi
        fi
    done

    echo ""
    echo "=================="
    echo -e "${BLUE}$(get_text "validate.summary_title")${NC}"
    echo "=================="
    echo "$(get_text "validate.total_configs"): $total"
    echo -e "$(get_text "validate.valid_configs"): ${GREEN}$valid${NC}"
    echo -e "$(get_text "validate.warning_configs"): ${YELLOW}$warnings${NC}"
    echo -e "$(get_text "validate.invalid_configs"): ${RED}$invalid${NC}"
    echo ""

    # 显示总结信息
    if [ $invalid -eq 0 ] && [ $warnings -eq 0 ]; then
        echo -e "${GREEN}✅ $(get_text "validate.all_valid")${NC}"
    elif [ $invalid -eq 0 ]; then
        echo -e "${YELLOW}⚠️ $(get_text "validate.found_warnings")${NC}"
    else
        echo -e "${RED}❌ $(get_text "validate.found_issues")${NC}"
    fi
}

# 验证API密钥格式
validate_api_key_format() {
    local api_key="$1"

    # 基础长度验证
    if [[ ${#api_key} -lt 20 ]]; then
        return 1
    fi

    # 检查是否只包含字母、数字和常见符号
    if [[ ! "$api_key" =~ ^[a-zA-Z0-9._-]+$ ]]; then
        return 1
    fi

    return 0
}

# 检查权限
check_permissions() {
    local issues=0

    # 检查配置目录权限
    if [ ! -d "$CONFIG_DIR" ]; then
        echo -e "${RED}❌ 配置目录不存在: $CONFIG_DIR${NC}"
        ((issues++))
    elif [ ! -r "$CONFIG_DIR" ]; then
        echo -e "${RED}❌ 配置目录不可读: $CONFIG_DIR${NC}"
        ((issues++))
    elif [ ! -w "$CONFIG_DIR" ]; then
        echo -e "${YELLOW}⚠️ 配置目录不可写: $CONFIG_DIR${NC}"
        ((issues++))
    fi

    # 检查活动配置文件权限
    if [ -f "$CLAUDE_CONFIG" ]; then
        if [ ! -r "$CLAUDE_CONFIG" ]; then
            echo -e "${RED}❌ 配置文件不可读: $CLAUDE_CONFIG${NC}"
            ((issues++))
        elif [ ! -w "$CLAUDE_CONFIG" ]; then
            echo -e "${YELLOW}⚠️ 配置文件不可写: $CLAUDE_CONFIG${NC}"
            ((issues++))
        fi
    fi

    # 检查备份目录权限
    if [ ! -d "$BACKUP_DIR" ]; then
        echo -e "${YELLOW}⚠️ 备份目录不存在，将创建${NC}"
        mkdir -p "$BACKUP_DIR" 2>/dev/null
    elif [ ! -w "$BACKUP_DIR" ]; then
        echo -e "${YELLOW}⚠️ 备份目录不可写: $BACKUP_DIR${NC}"
        ((issues++))
    fi

    return $issues
}

# 检查路由器状态
check_router_status() {
    echo -e "${BLUE}🔀 $(get_text "router.status_title")${NC}"
    echo "=================="
    echo ""

    local router_installed=false
    local router_running=false
    local router_port=3456

    # 检查路由器是否安装
    if command -v claude-code-router >/dev/null 2>&1; then
        router_installed=true
        echo -e "${GREEN}✅ $(get_text "router.installed")${NC}"
    else
        echo -e "${RED}❌ $(get_text "router.not_installed")${NC}"
    fi

    # 检查路由器是否运行
    if [ "$router_installed" = true ]; then
        echo -n "$(get_text "router.checking_running")... "
        if command -v nc >/dev/null 2>&1; then
            if nc -z localhost "$router_port" 2>/dev/null; then
                router_running=true
                echo -e "${GREEN}✅ $(get_text "router.running")${NC}"
            else
                echo -e "${YELLOW}⚠️ $(get_text "router.not_running")${NC}"
            fi
        elif command -v curl >/dev/null 2>&1; then
            if curl -s --connect-timeout 2 "http://localhost:$router_port/health" >/dev/null 2>&1; then
                router_running=true
                echo -e "${GREEN}✅ $(get_text "router.running")${NC}"
            else
                echo -e "${YELLOW}⚠️ $(get_text "router.not_running")${NC}"
            fi
        else
            echo -e "${YELLOW}⚠️ $(get_text "router.cannot_check")${NC}"
        fi
    fi

    echo ""
    echo "$(get_text "router.config_title"):"
    echo "------------------"

    # 检查路由器配置
    local router_configs=0
    for config in "$CONFIG_DIR"/*-router.json; do
        if [ -f "$config" ]; then
            ((router_configs++))
            local name=$(basename "$config" .json)
            local requires_router=$(jq -r '.requires_router // false' "$config" 2>/dev/null)
            if [ "$requires_router" = "true" ]; then
                echo -e "  ${GREEN}$name${NC}"
                if command -v jq >/dev/null 2>&1; then
                    local display_name=$(jq -r '.display_name // "未知"' "$config")
                    local thinking=$(jq -r '.thinking_enabled // false' "$config")
                    echo "    $display_name"
                    if [ "$thinking" = "true" ]; then
                        echo -e "    ${CYAN}🧠 $(get_text "router.thinking_enabled")${NC}"
                    fi
                fi
            fi
        fi
    done

    if [ $router_configs -eq 0 ]; then
        echo -e "  ${YELLOW}$(get_text "router.no_configs")${NC}"
    fi

    echo ""
    echo "=================="
    echo -e "${BLUE}$(get_text "router.summary_title")${NC}"
    echo "=================="

    if [ "$router_installed" = true ] && [ "$router_running" = true ]; then
        echo -e "${GREEN}✅ $(get_text "router.status.ready")${NC}"
        echo -e "${CYAN}💡 $(get_text "router.ready_hint")${NC}"
    elif [ "$router_installed" = true ]; then
        echo -e "${YELLOW}⚠️ $(get_text "router.status.installed_not_running")${NC}"
        echo -e "${CYAN}💡 $(get_text "router.start_hint")${NC}"
    else
        echo -e "${RED}❌ $(get_text "router.status.not_installed")${NC}"
        echo -e "${CYAN}💡 $(get_text "router.setup_hint")${NC}"
    fi
}

# 健康检查
health_check() {
    local config_name="${1:-$(get_current_config)}"
    local verbose="${2:-false}"

    echo -e "${BLUE}🏥 $(get_text "health.title")${NC}"
    echo "=================="
    echo ""

    local total_issues=0
    local total_warnings=0

    # 1. 系统检查
    echo -e "${CYAN}$(get_text "health.system_check")${NC}"
    echo "------------------"

    # 检查必要工具
    echo -n "检查必要工具 (jq)... "
    if command -v jq >/dev/null 2>&1; then
        echo -e "${GREEN}✅${NC}"
    else
        echo -e "${YELLOW}⚠️ 未安装${NC}"
        ((total_warnings++))
    fi

    echo -n "检查必要工具 (curl)... "
    if command -v curl >/dev/null 2>&1; then
        echo -e "${GREEN}✅${NC}"
    else
        echo -e "${YELLOW}⚠️ 未安装${NC}"
        ((total_warnings++))
    fi

    echo -n "检查配置目录权限... "
    if check_permissions; then
        echo -e "${GREEN}✅${NC}"
    else
        echo -e "${YELLOW}⚠️ 发现权限问题${NC}"
        ((total_issues++))
    fi

    echo ""

    # 2. 配置检查
    echo -e "${CYAN}$(get_text "health.config_check")${NC}"
    echo "------------------"

    local config_file="$CONFIG_DIR/$config_name.json"
    if [ ! -f "$config_file" ]; then
        echo -e "${RED}❌ $(format_text "health.config_not_found" "name" "$config_name")${NC}"
        ((total_issues++))
    else
        # 验证JSON格式
        echo -n "JSON格式验证... "
        if validate_json "$config_file"; then
            echo -e "${GREEN}✅${NC}"
        else
            echo -e "${RED}❌ 失败${NC}"
            ((total_issues++))
        fi

        # 验证配置完整性
        echo -n "配置完整性验证... "
        if validate_config_completeness "$config_file" >/dev/null 2>&1; then
            echo -e "${GREEN}✅${NC}"
        else
            echo -e "${YELLOW}⚠️ 有警告${NC}"
            ((total_warnings++))
        fi
    fi

    echo ""

    # 3. API检查
    if [ -f "$config_file" ] && command -v jq >/dev/null 2>&1; then
        echo -e "${CYAN}$(get_text "health.api_check")${NC}"
        echo "------------------"

        # 检查API密钥
        local auth_token=$(jq -r '.env.ANTHROPIC_AUTH_TOKEN // ""' "$config_file")
        echo -n "API密钥检查... "
        if [ -z "$auth_token" ] || [ "$auth_token" = "your-api-key-here" ]; then
            echo -e "${RED}❌ 未配置${NC}"
            ((total_issues++))
        elif ! validate_api_key_format "$auth_token"; then
            echo -e "${YELLOW}⚠️ 格式可能无效${NC}"
            ((total_warnings++))
        else
            echo -e "${GREEN}✅${NC}"
        fi

        # 检查API端点
        local base_url=$(jq -r '.env.ANTHROPIC_BASE_URL // ""' "$config_file")
        echo -n "API端点检查... "
        if [ -z "$base_url" ]; then
            echo -e "${RED}❌ 未配置${NC}"
            ((total_issues++))
        else
            echo -e "${GREEN}✅ $base_url${NC}"
        fi

        # 检查模型配置
        local model=$(jq -r '.env.ANTHROPIC_DEFAULT_SONNET_MODEL // ""' "$config_file")
        echo -n "模型配置检查... "
        if [ -z "$model" ]; then
            echo -e "${RED}❌ 未配置${NC}"
            ((total_issues++))
        else
            echo -e "${GREEN}✅ $model${NC}"
        fi

        # 检查API连接（仅在详细模式下）
        if [ "$verbose" = "true" ] && [ -n "$base_url" ] && command -v curl >/dev/null 2>&1; then
            echo ""
            echo -n "API连接测试... "
            if curl -s --connect-timeout 5 -I "$base_url" >/dev/null 2>&1; then
                echo -e "${GREEN}✅ 可达${NC}"
            else
                echo -e "${YELLOW}⚠️ 不可达${NC}"
                ((total_warnings++))
            fi
        fi
    fi

    echo ""
    echo "=================="
    echo -e "${BLUE}$(get_text "health.summary_title")${NC}"
    echo "=================="
    echo -e "严重问题: ${RED}$total_issues${NC}"
    echo -e "警告: ${YELLOW}$total_warnings${NC}"
    echo ""

    # 显示总结信息
    if [ $total_issues -eq 0 ] && [ $total_warnings -eq 0 ]; then
        echo -e "${GREEN}✅ $(get_text "health.status.healthy")${NC}"
    elif [ $total_issues -eq 0 ]; then
        echo -e "${YELLOW}⚠️ $(get_text "health.status.warning")${NC}"
    else
        echo -e "${RED}❌ $(get_text "health.status.critical")${NC}"
    fi
}

# 显示帮助信息
show_help() {
    echo -e "${BLUE}$(get_text "app.title")${NC}"
    echo ""
    echo "$(get_text "help.title")"

    # 显示用法信息 - 简化处理，直接读取JSON数组
    if command -v jq >/dev/null 2>&1; then
        local resource_file="$I18N_DIR/$CURRENT_LANGUAGE.json"
        if [ -f "$resource_file" ]; then
            echo ""
            # 用法信息
            local usage_items=$(jq -r '.help.usage[]' "$resource_file" 2>/dev/null)
            while IFS= read -r line; do
                if [ -n "$line" ]; then
                    echo "  $(format_text_line "$line" "0" "$0")"
                fi
            done <<< "$usage_items"

            echo ""
            echo "$(get_text "help.aliases_title")"
            # 别名信息
            local alias_items=$(jq -r '.help.aliases[]' "$resource_file" 2>/dev/null)
            while IFS= read -r line; do
                if [ -n "$line" ]; then
                    echo "  $line"
                fi
            done <<< "$alias_items"

            echo ""
            echo "$(get_text "help.providers_title")"
            # 供应商信息
            local provider_items=$(jq -r '.help.providers[]' "$resource_file" 2>/dev/null)
            while IFS= read -r line; do
                if [ -n "$line" ]; then
                    echo "  $line"
                fi
            done <<< "$provider_items"

            echo ""
            echo "$(get_text "help.examples_title")"
            # 示例信息
            local example_items=$(jq -r '.help.examples[]' "$resource_file" 2>/dev/null)
            while IFS= read -r line; do
                if [ -n "$line" ]; then
                    echo "  $(format_text_line "$line" "0" "$0")"
                fi
            done <<< "$example_items"
        fi
    fi
}

# 切换语言
switch_language_to() {
    local target_lang="$1"

    # 验证语言资源是否存在
    local resource_file="$I18N_DIR/$target_lang.json"
    if [ ! -f "$resource_file" ]; then
        echo -e "${RED}❌ $(format_text "messages.errors.config_not_found" "path" "$resource_file")${NC}"
        return 1
    fi

    # 切换语言
    if set_language "$target_lang"; then
        echo -e "${GREEN}$(get_text "messages.success.language_valid")${NC}"
        echo -e "${CYAN}$(get_text "messages.success.language_info")${NC}"

        # 可选：设置环境变量以便后续会话记住语言选择
        export CLAUDE_LANG="$target_lang"

        # 显示当前语言的帮助信息
        echo ""
        show_help
    else
        echo -e "${RED}❌ $(format_text "messages.errors.config_not_found" "path" "$resource_file")${NC}"
        return 1
    fi
}

# 辅助函数：格式化文本行（支持简单的$0替换）
format_text_line() {
    local line="$1"
    local param_name="$2"
    local param_value="$3"

    if [ -n "$param_value" ]; then
        line="${line//\$$param_name/$param_value}"
    fi

    echo "$line"
}

# 设置API密钥
set_key_config() {
    local config_name="$1"
    local api_key="$2"

    # 参数验证
    if [ -z "$config_name" ] || [ -z "$api_key" ]; then
        if [ -z "$config_name" ]; then
            echo -e "${RED}$(get_text "messages.errors.set_key_no_config")${NC}"
        else
            echo -e "${RED}$(get_text "messages.errors.set_key_no_api_key")${NC}"
        fi
        echo "$(format_text "messages.errors.set_key_usage" "0" "$0")"
        return 1
    fi

    ensure_dirs

    # 查找配置文件
    local config_file="$CONFIG_DIR/$config_name.json"
    if [ ! -f "$config_file" ]; then
        echo -e "${RED}$(format_text "messages.errors.set_key_config_not_found" "name" "$config_name")${NC}"
        return 1
    fi

    # 验证JSON格式
    if ! validate_json "$config_file"; then
        echo -e "${RED}$(get_text "messages.errors.set_key_json_error")${NC}"
        return 1
    fi

    # 基础密钥格式验证
    if [[ ${#api_key} -lt 20 ]]; then
        echo -e "${RED}$(get_text "messages.errors.set_key_invalid_format")${NC}"
        return 1
    fi

    # 创建备份
    local backup_file="$config_file.backup.$(date +%Y%m%d_%H%M%S)"
    if ! cp "$config_file" "$backup_file" 2>/dev/null; then
        echo -e "${YELLOW}$(get_text "messages.errors.set_key_backup_failed")${NC}"
    else
        echo -e "${CYAN}$(format_text "status.config_backed_up" "path" "$backup_file")${NC}"
    fi

    # 使用jq更新API密钥
    if command -v jq >/dev/null 2>&1; then
        if jq --arg api_key "$api_key" '.env.ANTHROPIC_AUTH_TOKEN = $api_key' "$config_file" > "$config_file.tmp" && mv "$config_file.tmp" "$config_file"; then
            echo -e "${GREEN}$(format_text "messages.errors.set_key_success" "name" "$config_name")${NC}"
            return 0
        else
            echo -e "${RED}$(get_text "messages.errors.set_key_write_failed")${NC}"
            # 尝试恢复备份
            if [ -f "$backup_file" ]; then
                mv "$backup_file" "$config_file"
            fi
            return 1
        fi
    else
        # 如果没有jq，使用sed作为后备方案
        if sed -i "s/\"ANTHROPIC_AUTH_TOKEN\":[[:space:]]*\"[^\"]*\"/\"ANTHROPIC_AUTH_TOKEN\": \"$api_key\"/" "$config_file"; then
            echo -e "${GREEN}$(format_text "messages.errors.set_key_success" "name" "$config_name")${NC}"
            echo -e "${YELLOW}$(get_text "messages.warnings.jq_required")${NC}"
            return 0
        else
            echo -e "${RED}$(get_text "messages.errors.set_key_write_failed")${NC}"
            # 尝试恢复备份
            if [ -f "$backup_file" ]; then
                mv "$backup_file" "$config_file"
            fi
            return 1
        fi
    fi
}

# 交互式批量设置API密钥
setup_keys_interactive() {
    echo -e "${BLUE}$(get_text "key_management.setup_title")${NC}"
    echo "$(get_text "key_management.setup_intro")"
    echo ""

    # 获取所有需要设置密钥的配置
    local configs_needing_key=()
    for config_file in "$CONFIG_DIR"/*.json; do
        if [ -f "$config_file" ]; then
            local config_name=$(basename "$config_file" .json)
            # 跳过语言配置
            case "$config_name" in
                zh-ui|en-ui|*.tmp|*.backup*)
                    continue
                    ;;
            esac

            # 检查是否已有API密钥
            if command -v jq >/dev/null 2>&1; then
                local current_key=$(jq -r '.env.ANTHROPIC_AUTH_TOKEN // ""' "$config_file" 2>/dev/null)
                if [[ -z "$current_key" || "$current_key" == "your-api-key-here" || "$current_key" == "sk-" ]]; then
                    configs_needing_key+=("$config_name")
                fi
            else
                # 如果没有jq，将所有配置都加入
                configs_needing_key+=("$config_name")
            fi
        fi
    done

    if [ ${#configs_needing_key[@]} -eq 0 ]; then
        echo -e "${GREEN}$(get_text "key_management.no_configs")${NC}"
        return 0
    fi

    local success_count=0
    local total_count=${#configs_needing_key[@]}

    for config_name in "${configs_needing_key[@]}"; do
        echo ""
        echo -e "${CYAN}$(format_text "key_management.setting_key" "name" "$config_name")${NC}"

        # 检查当前密钥状态
        local config_file="$CONFIG_DIR/$config_name.json"
        local current_key=""
        if command -v jq >/dev/null 2>&1; then
            current_key=$(jq -r '.env.ANTHROPIC_AUTH_TOKEN // ""' "$config_file" 2>/dev/null)
        fi

        if [[ -n "$current_key" && "$current_key" != "your-api-key-here" && "$current_key" != "sk-" ]]; then
            echo "$(format_text "key_management.skip_existing" "name" "$config_name")"
            echo -n "重新设置？ (y/n): "
            read -r response
            case "$response" in
                [Yy]*)
                    # 继续
                    ;;
                *)
                    echo "跳过 $config_name"
                    continue
                    ;;
            esac
        fi

        # 输入API密钥
        local api_key=""
        while [[ -z "$api_key" ]]; do
            echo -n "$(format_text "key_management.input_key_prompt" "name" "$config_name")"
            read -s api_key
            echo ""

            if [[ -z "$api_key" ]]; then
                echo -e "${RED}API密钥不能为空，请重新输入${NC}"
                continue
            fi

            if [[ ${#api_key} -lt 20 ]]; then
                echo -e "${RED}API密钥格式可能无效，确定要继续吗？ (y/n): ${NC}"
                read -r confirm
                case "$confirm" in
                    [Yy]*)
                        # 继续
                        ;;
                    *)
                        api_key=""
                        continue
                        ;;
                esac
            fi
        done

        # 确认设置
        local key_preview="${api_key:0:10}"
        echo -n "$(format_text "key_management.confirm_key" "key_preview" "$key_preview")"
        read -r confirm
        case "$confirm" in
            [Yy]*)
                if set_key_config "$config_name" "$api_key"; then
                    ((success_count++))
                    echo -e "${GREEN}✅ $config_name 密钥设置成功${NC}"
                else
                    echo -e "${RED}❌ $config_name 密钥设置失败${NC}"
                fi
                ;;
            [Nn]*)
                echo "已取消设置 $config_name"
                continue
                ;;
            [Rr]*)
                # 重新输入
                    api_key=""
                    while [[ -z "$api_key" ]]; do
                        echo -n "$(format_text "key_management.input_key_prompt" "name" "$config_name")"
                        read -s api_key
                        echo ""

                        if [[ -z "$api_key" ]]; then
                            echo -e "${RED}API密钥不能为空，请重新输入${NC}"
                            continue
                        fi
                    done

                    if set_key_config "$config_name" "$api_key"; then
                        ((success_count++))
                        echo -e "${GREEN}✅ $config_name 密钥设置成功${NC}"
                    else
                        echo -e "${RED}❌ $config_name 密钥设置失败${NC}"
                    fi
                ;;
            [Qq]*)
                echo "$(get_text "key_management.operation_cancelled")"
                break
                ;;
            *)
                echo "$(get_text "key_management.invalid_response")"
                continue
                ;;
        esac
    done

    echo ""
    echo -e "${GREEN}$(get_text "key_management.all_done")${NC}"
    echo "$(format_text "key_management.setup_summary" "success" "$success_count" "total" "$total_count")"
}

# 获取当前配置信息
get_current_config() {
    if [ -f "$ACTIVE_MARKER" ]; then
        cat "$ACTIVE_MARKER"
    else
        echo "default"
    fi
}

# 显示配置状态
show_status() {
    echo -e "${BLUE}$(get_text "status.title")${NC}"
    echo "=================="

    # 检查配置文件是否存在，包括备用路径
    local actual_config="$CLAUDE_CONFIG"
    if [ ! -f "$CLAUDE_CONFIG" ] && [ -f "$ALTERNATIVE_CLAUDE_CONFIG" ]; then
        actual_config="$ALTERNATIVE_CLAUDE_CONFIG"
        echo -e "${YELLOW}$(format_text "status.alternative_path" "path" "$ALTERNATIVE_CLAUDE_CONFIG")${NC}"
    fi

    if [ ! -f "$actual_config" ]; then
        echo -e "${RED}$(format_text "status.config_not_found" "path" "$CLAUDE_CONFIG")${NC}"
        echo -e "${RED}   $(get_text "status.main_path"): $CLAUDE_CONFIG${NC}"
        echo -e "${RED}   $(get_text "status.backup_path"): $ALTERNATIVE_CLAUDE_CONFIG${NC}"
        return 1
    fi

    local current=$(get_current_config)
    echo -e "${GREEN}$(format_text "status.current" "name" "$current")${NC}"

    if command -v jq >/dev/null 2>&1; then
        echo ""
        echo "$(get_text "status.api_config")"
        jq -r '.env | to_entries[] | "  \(.key): \(.value)"' "$actual_config" 2>/dev/null || echo "  $(get_text "status.config_parse_failed")"
    else
        echo ""
        echo "$(format_text "status.config_exists" "path" "$CLAUDE_CONFIG")"
    fi
}

# 列出所有配置
list_configs() {
    echo -e "${BLUE}$(get_text "status.models_title")${NC}"
    echo "=================="

    local current=$(get_current_config)

    # 显示当前配置
    if [ -f "$CLAUDE_CONFIG" ]; then
        echo -e "${GREEN}$(format_text "status.current_config" "name" "$current")${NC}"
        if command -v jq >/dev/null 2>&1; then
            local base_url=$(jq -r '.env.ANTHROPIC_BASE_URL // "未知"' "$CLAUDE_CONFIG")
            local model=$(jq -r '.env.ANTHROPIC_DEFAULT_SONNET_MODEL // "未知"' "$CLAUDE_CONFIG")
            echo "$(format_text "status.base_url" "url" "$base_url")"
            echo "$(format_text "status.model" "model" "$model")"
        fi
        echo ""
    fi

    # 显示可用配置
    if [ "$(ls -A "$CONFIG_DIR" 2>/dev/null)" ]; then
        echo -e "${YELLOW}$(get_text "status.available_configs")${NC}"
        local i=1
        for config in "$CONFIG_DIR"/*.json; do
            if [ -f "$config" ]; then
                local name=$(basename "$config" .json)

                # 排除特殊配置类型（与别名生成逻辑保持一致）
                case "$name" in
                    zh-ui|en-ui|test-*|*.tmp|*.backup*)
                        continue
                        ;;
                esac
                local marker=" "
                if [ "$name" = "$current" ]; then
                    marker="*"
                    echo -e "${GREEN}$marker $i. $name (当前)${NC}"
                else
                    echo "  $i. $name"
                fi

                if command -v jq >/dev/null 2>&1; then
                    local base_url=$(jq -r '.env.ANTHROPIC_BASE_URL // "未知"' "$config")
                    local model=$(jq -r '.env.ANTHROPIC_DEFAULT_SONNET_MODEL // "未知"' "$config")
                    echo "     BASE_URL: $base_url"
                    echo "     MODEL: $model"
                fi
                echo ""
                ((i++))
            fi
        done
    else
        echo -e "${YELLOW}没有找到配置文件${NC}"
        echo "配置目录: $CONFIG_DIR"
    fi
}

# 备份当前配置
backup_config() {
    if [ ! -f "$CLAUDE_CONFIG" ]; then
        echo -e "${RED}❌ 没有可备份的配置文件${NC}"
        return 1
    fi

    local backup_file="$BACKUP_DIR/settings.backup.$(date +%Y%m%d_%H%M%S).json"
    if cp "$CLAUDE_CONFIG" "$backup_file"; then
        echo -e "${GREEN}✅ 配置已备份到: $backup_file${NC}"
    else
        echo -e "${RED}❌ 备份失败: 无法写入 $backup_file${NC}"
        return 1
    fi
}

# 切换配置
switch_config() {
    local target="$1"

    if [ -z "$target" ]; then
        echo -e "${RED}$(get_text "messages.errors.no_config_specified")${NC}"
        echo "$(format_text "messages.errors.usage_switch" "0" "$0")"
        echo "$(format_text "messages.errors.use_list" "0" "$0")"
        return 1
    fi

    ensure_dirs
    check_config_file || return 1

    # 处理数字编号
    if [[ "$target" =~ ^[0-9]+$ ]]; then
        local configs=("$CONFIG_DIR"/*.json)
        local index=$((target - 1))
        if [ $index -ge 0 ] && [ $index -lt ${#configs[@]} ] && [ -f "${configs[$index]}" ]; then
            target=$(basename "${configs[$index]}" .json)
        else
            echo -e "${RED}$(format_text "messages.errors.config_number_invalid" "number" "$target")${NC}"
            echo "$(format_text "messages.errors.use_list" "0" "$0")"
            return 1
        fi
    fi

    # 查找配置文件
    local config_file="$CONFIG_DIR/$target.json"

    if [ ! -f "$config_file" ]; then
        echo -e "${RED}$(format_text "messages.errors.config_name_invalid" "name" "$target")${NC}"
        echo "$(format_text "messages.errors.use_list" "0" "$0")"
        return 1
    fi

    # 验证配置文件格式
    if ! validate_json "$config_file"; then
        echo -e "${RED}$(format_text "messages.errors.config_format_invalid" "path" "$config_file")${NC}"
        return 1
    fi

    # 验证配置完整性
    if ! validate_config_completeness "$config_file"; then
        echo -e "${RED}$(get_text "messages.errors.config_validation_failed")${NC}"
        return 1
    fi

    # 可选：验证API连接性
    if [ "${VALIDATE_API:-false}" = "true" ]; then
        local base_url=$(jq -r '.env.ANTHROPIC_BASE_URL // ""' "$config_file")
        validate_api_connectivity "$base_url"
    fi

    # 切换配置并应用环境变量覆盖
    apply_config_with_env_overrides "$config_file" "$target"

    if ! printf '%s' "$target" > "$ACTIVE_MARKER"; then
        echo -e "${YELLOW}⚠️ 无法更新活动配置标记 ($ACTIVE_MARKER)，请手动检查权限${NC}"
    fi

    echo -e "${GREEN}✅ 已切换到配置: $target${NC}"

    # 显示新配置信息
    show_config_info "$config_file"

    echo ""
    echo -e "${CYAN}💡 重启Claude CLI以应用新配置${NC}"
}

# 应用配置并处理环境变量覆盖
apply_config_with_env_overrides() {
    local config_file="$1"
    local config_name="$2"
    local temp_config=$(mktemp)

    # 读取基础配置
    if command -v jq >/dev/null 2>&1; then
        # 使用jq处理环境变量覆盖
        jq '
            .env |= with_entries(
                if .key == "ANTHROPIC_AUTH_TOKEN" and ($ENV_ANTHROPIC_AUTH_TOKEN | length) > 0 then
                    .value = $ENV_ANTHROPIC_AUTH_TOKEN
                elif .key == "ANTHROPIC_BASE_URL" and ($ENV_ANTHROPIC_BASE_URL | length) > 0 then
                    .value = $ENV_ANTHROPIC_BASE_URL
                elif .key == "ANTHROPIC_DEFAULT_OPUS_MODEL" and ($ENV_ANTHROPIC_DEFAULT_OPUS_MODEL | length) > 0 then
                    .value = $ENV_ANTHROPIC_DEFAULT_OPUS_MODEL
                elif .key == "ANTHROPIC_DEFAULT_SONNET_MODEL" and ($ENV_ANTHROPIC_DEFAULT_SONNET_MODEL | length) > 0 then
                    .value = $ENV_ANTHROPIC_DEFAULT_SONNET_MODEL
                elif .key == "ANTHROPIC_DEFAULT_HAIKU_MODEL" and ($ENV_ANTHROPIC_DEFAULT_HAIKU_MODEL | length) > 0 then
                    .value = $ENV_ANTHROPIC_DEFAULT_HAIKU_MODEL
                else
                    .
                end
            )
        ' --arg ENV_ANTHROPIC_AUTH_TOKEN "${ANTHROPIC_AUTH_TOKEN:-}" \
           --arg ENV_ANTHROPIC_BASE_URL "${ANTHROPIC_BASE_URL:-}" \
           --arg ENV_ANTHROPIC_DEFAULT_OPUS_MODEL "${ANTHROPIC_DEFAULT_OPUS_MODEL:-}" \
           --arg ENV_ANTHROPIC_DEFAULT_SONNET_MODEL "${ANTHROPIC_DEFAULT_SONNET_MODEL:-}" \
           --arg ENV_ANTHROPIC_DEFAULT_HAIKU_MODEL "${ANTHROPIC_DEFAULT_HAIKU_MODEL:-}" \
           "$config_file" > "$temp_config"

        # 检查是否有环境变量覆盖
        local overrides_active=false
        [ -n "${ANTHROPIC_AUTH_TOKEN:-}" ] && overrides_active=true
        [ -n "${ANTHROPIC_BASE_URL:-}" ] && overrides_active=true
        [ -n "${ANTHROPIC_DEFAULT_SONNET_MODEL:-}" ] && overrides_active=true

        if [ "$overrides_active" = true ]; then
            echo -e "${YELLOW}🔧 检测到环境变量覆盖，正在应用...${NC}"
        fi
    else
        # 无jq时简单复制
        cp "$config_file" "$temp_config"
    fi

    # 应用配置到Claude settings.json
    if ! cp "$temp_config" "$CLAUDE_CONFIG" 2>/dev/null; then
        echo -e "${YELLOW}⚠️  主配置路径写入失败，尝试备用路径${NC}"

        # 尝试使用备用配置路径
        mkdir -p "$(dirname "$ALTERNATIVE_CLAUDE_CONFIG")"
        if ! cp "$temp_config" "$ALTERNATIVE_CLAUDE_CONFIG"; then
            echo -e "${RED}❌ 切换失败: 无法写入配置文件${NC}"
            echo -e "${RED}   主路径: $CLAUDE_CONFIG${NC}"
            echo -e "${RED}   备用路径: $ALTERNATIVE_CLAUDE_CONFIG${NC}"
            echo ""
            echo -e "${CYAN}💡 可能的解决方案:${NC}"
            echo "   1. 检查 ~/.claude/ 目录权限: chmod 755 ~/.claude"
            echo "   2. 运行权限诊断脚本: ./diagnose_permissions.sh"
            echo "   3. 尝试手动设置环境变量:"
            echo "      export ANTHROPIC_AUTH_TOKEN='your-api-key'"
            echo "      export ANTHROPIC_BASE_URL='your-base-url'"
            echo ""
            rm -f "$temp_config"
            return 1
        else
            echo -e "${GREEN}✅ 已使用备用配置路径: $ALTERNATIVE_CLAUDE_CONFIG${NC}"
            echo -e "${YELLOW}⚠️  请设置环境变量 CLAUDE_CONFIG_DIR=$HOME/.claude-config${NC}"
            CLAUDE_CONFIG="$ALTERNATIVE_CLAUDE_CONFIG"
        fi
    fi

    rm -f "$temp_config"
}

# 显示配置信息（包含覆盖状态）
show_config_info() {
    local config_file="$1"

    if ! command -v jq >/dev/null 2>&1; then
        echo "  (需要安装jq以显示详细信息)"
        return
    fi

    # 检查实际应用的配置（从settings.json）
    local base_url=$(jq -r '.env.ANTHROPIC_BASE_URL // "未知"' "$CLAUDE_CONFIG")
    local model=$(jq -r '.env.ANTHROPIC_DEFAULT_SONNET_MODEL // "未知"' "$CLAUDE_CONFIG")

    echo "  BASE_URL: $base_url"
    echo "  MODEL: $model"

    # 检查环境变量覆盖
    local overrides=()
    local env_token="${ANTHROPIC_AUTH_TOKEN:-}"
    local env_base_url="${ANTHROPIC_BASE_URL:-}"
    local env_sonnet="${ANTHROPIC_DEFAULT_SONNET_MODEL:-}"
    local env_haiku="${ANTHROPIC_DEFAULT_HAIKU_MODEL:-}"

    # 获取原始配置值
    local orig_token=$(jq -r '.env.ANTHROPIC_AUTH_TOKEN // ""' "$config_file")
    local orig_base_url=$(jq -r '.env.ANTHROPIC_BASE_URL // ""' "$config_file")
    local orig_sonnet=$(jq -r '.env.ANTHROPIC_DEFAULT_SONNET_MODEL // ""' "$config_file")
    local orig_haiku=$(jq -r '.env.ANTHROPIC_DEFAULT_HAIKU_MODEL // ""' "$config_file")

    # 检查哪些环境变量被覆盖了
    [ -n "$env_token" ] && [ "$env_token" != "$orig_token" ] && overrides+=("AUTH_TOKEN")
    [ -n "$env_base_url" ] && [ "$env_base_url" != "$orig_base_url" ] && overrides+=("BASE_URL")
    [ -n "$env_sonnet" ] && [ "$env_sonnet" != "$orig_sonnet" ] && overrides+=("SONNET_MODEL")
    [ -n "$env_haiku" ] && [ "$env_haiku" != "$orig_haiku" ] && overrides+=("HAIKU_MODEL")

    if [ ${#overrides[@]} -gt 0 ]; then
        echo -e "  ${YELLOW}🔧 环境变量覆盖: ${overrides[*]}${NC}"
    fi

    # 显示模型信息（如果存在）
    if jq -e '.models' "$config_file" >/dev/null 2>&1; then
        local model_count=$(jq -r '.models | keys | length' "$config_file")
        if [ "$model_count" -gt 1 ]; then
            echo "  可用模型: $model_count 个"
        fi
    fi
}

# 保存当前配置
save_config() {
    local name="$1"

    if [ -z "$name" ]; then
        echo -e "${RED}❌ 请提供配置名称${NC}"
        echo "用法: $0 save <配置名称>"
        return 1
    fi

    # 检查名称是否有效
    if [[ ! "$name" =~ ^[a-zA-Z0-9_-]+$ ]]; then
        echo -e "${RED}❌ 配置名称只能包含字母、数字、下划线和连字符${NC}"
        return 1
    fi

    # 检查配置文件是否已存在
    local config_file="$CONFIG_DIR/$name.json"
    if [ -f "$config_file" ]; then
        echo -e "${YELLOW}⚠️ 配置 '$name' 已存在${NC}"
        if ! read -t 30 -p "是否覆盖? (y/N): " -r 2>/dev/null; then
            echo -e "${RED}❌ 输入超时，操作取消${NC}"
            return 1
        fi
        if [[ ! $REPLY =~ ^[Yy]$ ]]; then
            echo "操作已取消"
            return 0
        fi
    fi

    # 检查当前配置文件是否存在
    if [ ! -f "$CLAUDE_CONFIG" ]; then
        echo -e "${RED}❌ 当前Claude配置文件不存在: $CLAUDE_CONFIG${NC}"
        return 1
    fi

    # 验证当前配置文件格式
    if ! validate_json "$CLAUDE_CONFIG"; then
        echo -e "${RED}❌ 当前配置文件格式无效，无法保存${NC}"
        return 1
    fi

    # 创建标准化的配置文件结构
    local temp_file=$(mktemp)
    if command -v jq >/dev/null 2>&1; then
        # 使用jq创建标准结构
        local current_time=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

        jq --arg name "$name" \
           --arg display_name "配置: $name" \
           --arg description "用户自定义配置" \
           --arg created_time "$current_time" \
           --arg updated_time "$current_time" \
           --arg version "2.0" '
            {
                name: $name,
                display_name: $display_name,
                provider: (.provider // "custom"),
                description: $description,
                env: .env,
                models: (
                    if .models then
                        .models
                    else
                        {
                            (.default_model // "default"): "默认模型"
                        }
                    end
                ),
                default_model: (.default_model // "default"),
                metadata: {
                    created_at: $created_time,
                    updated_at: $updated_time,
                    version: $version,
                    author: "user",
                    source: "claude-switch-save"
                }
            }
        ' "$CLAUDE_CONFIG" > "$temp_file"
    else
        # 简单复制（无jq时）- 添加基本结构注释
        cp "$CLAUDE_CONFIG" "$temp_file"
        echo ""
        echo "# 注意：建议安装 jq 工具以获得更好的配置文件格式支持" >> "$temp_file"
    fi

    # 保存配置文件
    if mv "$temp_file" "$config_file"; then
        echo -e "${GREEN}✅ 配置已保存: $name${NC}"
        echo "  位置: $config_file"

        # 显示保存的配置信息
        if command -v jq >/dev/null 2>&1; then
            local base_url=$(jq -r '.env.ANTHROPIC_BASE_URL // "未知"' "$config_file")
            local model=$(jq -r '.default_model // "未知"' "$config_file")
            echo "  BASE_URL: $base_url"
            echo "  默认模型: $model"
        fi
    else
        echo -e "${RED}❌ 保存配置失败${NC}"
        rm -f "$temp_file"
        return 1
    fi
}

# 删除配置
delete_config() {
    local name="$1"

    if [ -z "$name" ]; then
        echo -e "${RED}❌ 请提供要删除的配置名称${NC}"
        echo "用法: $0 delete <配置名称>"
        echo "使用 '$0 list' 查看可用配置"
        return 1
    fi

    # 检查是否为当前活动配置
    if [ -f "$ACTIVE_MARKER" ] && [ "$(cat "$ACTIVE_MARKER")" = "$name" ]; then
        echo -e "${RED}❌ 无法删除当前活动配置 '$name'${NC}"
        echo "请先切换到其他配置再删除此配置"
        return 1
    fi

    local config_file="$CONFIG_DIR/$name.json"

    if [ ! -f "$config_file" ]; then
        echo -e "${RED}❌ 配置 '$name' 不存在${NC}"
        echo "使用 '$0 list' 查看可用配置"
        return 1
    fi

    # 确认删除操作
    echo -e "${YELLOW}⚠️ 即将删除配置: $name${NC}"
    echo "  文件: $config_file"
    if ! read -t 30 -p "确认删除? (输入 'yes' 确认): " -r 2>/dev/null; then
        echo -e "${RED}❌ 输入超时，操作取消${NC}"
        return 1
    fi
    if [[ "$REPLY" != "yes" ]]; then
        echo "操作已取消"
        return 0
    fi

    # 创建备份（可选）
    local backup_file="$BACKUP_DIR/${name}_deleted_$(date +%Y%m%d_%H%M%S).json"
    if cp "$config_file" "$backup_file"; then
        echo -e "${BLUE}📦 已创建备份: $backup_file${NC}"
    fi

    # 删除配置文件
    if rm "$config_file"; then
        echo -e "${GREEN}✅ 配置 '$name' 已删除${NC}"
    else
        echo -e "${RED}❌ 删除配置失败${NC}"
        return 1
    fi
}

# 交互式创建新配置
create_config_interactive() {
    echo -e "${BLUE}🔧 交互式配置创建向导${NC}"
    echo "========================"
    echo ""

    # 步骤1：选择配置名称
    while true; do
        # 添加超时和输入检查
        if [ -t 0 ]; then
            # 交互式输入
            if ! read -t 60 -p "请输入配置名称 (只允许字母、数字、下划线和连字符): " config_name 2>/dev/null; then
                echo -e "${RED}❌ 输入超时或无法读取输入，正在退出...${NC}"
                return 1
            fi
        else
            # 管道输入
            read -r config_name
        fi

        # 检查是否为空
        if [ -z "$config_name" ]; then
            echo -e "${RED}❌ 配置名称不能为空${NC}"
            continue
        fi
        # 检查格式
        if [[ "$config_name" =~ ^[a-zA-Z0-9_-]+$ ]]; then
            local config_file="$CONFIG_DIR/$config_name.json"
            if [ -f "$config_file" ]; then
                echo -e "${YELLOW}⚠️ 配置 '$config_name' 已存在${NC}"
                if ! read -t 30 -p "是否覆盖? (y/N): " -r 2>/dev/null; then
                    echo -e "${RED}❌ 输入超时，操作取消${NC}"
                    return 1
                fi
                if [[ ! $REPLY =~ ^[Yy]$ ]]; then
                    echo "操作已取消"
                    return 0
                fi
            fi
            break
        else
            echo -e "${RED}❌ 配置名称格式无效，只允许字母、数字、下划线和连字符${NC}"
        fi
    done

    # 步骤2：选择配置方式
    echo ""
    echo "请选择配置方式:"
    echo "  1. 基于现有提供商模板"
    echo "  2. 基于当前Claude配置"
    echo "  3. 完全自定义配置"
    echo ""
    if [ -t 0 ]; then
        # 交互式输入
        if ! read -t 30 -p "请选择 (1-3): " -n 1 -r 2>/dev/null; then
            echo -e "${RED}❌ 输入超时，操作取消${NC}"
            return 1
        fi
    else
        # 管道输入
        read -r REPLY
    fi
    echo ""

    case $REPLY in
        1)
            create_from_template "$config_name"
            ;;
        2)
            create_from_current "$config_name"
            ;;
        3)
            create_custom "$config_name"
            ;;
        *)
            echo -e "${RED}❌ 无效选择${NC}"
            return 1
            ;;
    esac
}

# 基于模板创建配置
create_from_template() {
    local config_name="$1"

    echo ""
    echo -e "${BLUE}📋 可用的提供商模板:${NC}"
    echo "===================="

    local all_templates=(
        "anthropic-official:Claude官方"
        "glm:智谱AI"
        "kimi2:月之暗面"
        "kimi-cn:Kimi中国版"
        "kimi-global:Kimi国际版"
        "minimax:MiniMax"
        "deepseek:DeepSeek"
        "qwen:通义千问"
        "hunyuan:腾讯混元"
        "doubao:豆包"
        "ernie:文心一言"
        "sensenova:商汤SenseNova"
        "zh-ui:中文界面"
        "en-ui:英文界面"
    )

    # 过滤出实际存在的模板文件
    local templates=()
    local template_indices=()

    for i in "${!all_templates[@]}"; do
        local key="${all_templates[$i]%%:*}"
        local name="${all_templates[$i]##*:}"
        local template_file="$CONFIG_DIR/$key.json"

        if [ -f "$template_file" ]; then
            templates+=("$key:$name")
            template_indices+=($i)

            # 为语言模板添加特殊标识
            if [[ "$key" == "zh-ui" || "$key" == "en-ui" ]]; then
                echo "  $((i+1)). $name ($key) [语言]"
            else
                echo "  $((i+1)). $name ($key)"
            fi
        fi
    done

    # 检查是否有可用模板
    if [ ${#templates[@]} -eq 0 ]; then
        echo -e "${RED}❌ 没有找到可用的模板文件${NC}"
        echo "  请确保 $CONFIG_DIR/ 目录下有模板文件"
        return 1
    fi

    echo ""
    if [ -t 0 ]; then
        # 交互式输入 - 需要显示原始索引给用户选择
        if ! read -t 30 -p "请选择模板 (1-${#all_templates[@]}): " -r 2>/dev/null; then
            echo -e "${RED}❌ 输入超时，操作取消${NC}"
            return 1
        fi
    else
        # 管道输入
        read -r REPLY
    fi
    echo ""

    # 验证选择
    if [[ "$REPLY" =~ ^[0-9]+$ ]] && [ "$REPLY" -ge 1 ] && [ "$REPLY" -le ${#all_templates[@]} ]; then
        local selected_template="${all_templates[$((REPLY-1))]}"
        local template_key="${selected_template%%:*}"
        local template_file="$CONFIG_DIR/$template_key.json"

        # 再次检查模板文件是否存在
        if [ -f "$template_file" ]; then
            echo "正在基于模板创建配置..."
            cp "$template_file" "$CONFIG_DIR/$config_name.json"

            # 更新配置名称和元数据
            if command -v jq >/dev/null 2>&1; then
                jq --arg name "$config_name" '
                    .name = $name |
                    .display_name = ("配置: " + $name) |
                    .description = ("基于模板创建的配置")
                ' "$CONFIG_DIR/$config_name.json" > "$CONFIG_DIR/$config_name.json.tmp" && \
                mv "$CONFIG_DIR/$config_name.json.tmp" "$CONFIG_DIR/$config_name.json"
            fi

            echo -e "${GREEN}✅ 配置 '$config_name' 已创建${NC}"
            echo "  请编辑配置文件以设置您的API密钥和其他参数"
            echo "  配置文件位置: $CONFIG_DIR/$config_name.json"
        else
            echo -e "${RED}❌ 模板文件 '$template_key.json' 不存在${NC}"
            echo "  可用模板文件："
            ls -la "$CONFIG_DIR"/*.json 2>/dev/null | grep -o '[^/]*\.json$' | sed 's/^/    /' || echo "    无模板文件"
            return 1
        fi
    else
        echo -e "${RED}❌ 无效选择，请输入 1-${#all_templates[@]} 之间的数字${NC}"
        return 1
    fi
}

# 基于当前配置创建
create_from_current() {
    local config_name="$1"

    if [ ! -f "$CLAUDE_CONFIG" ]; then
        echo -e "${RED}❌ 当前Claude配置文件不存在${NC}"
        echo "请先使用Claude CLI进行一次连接以创建配置文件"
        return 1
    fi

    echo "正在基于当前配置创建新配置..."
    cp "$CLAUDE_CONFIG" "$CONFIG_DIR/$config_name.json"

    # 更新配置名称和元数据
    if command -v jq >/dev/null 2>&1; then
        jq --arg name "$config_name" '
            .name = $name |
            .display_name = ("配置: " + $name) |
            .description = ("基于当前Claude配置保存")
        ' "$CONFIG_DIR/$config_name.json" > "$CONFIG_DIR/$config_name.json.tmp" && \
        mv "$CONFIG_DIR/$config_name.json.tmp" "$CONFIG_DIR/$config_name.json"
    fi

    echo -e "${GREEN}✅ 配置 '$config_name' 已保存${NC}"
    echo "  配置文件位置: $CONFIG_DIR/$config_name.json"
}

# 完全自定义配置
create_custom() {
    local config_name="$1"

    echo ""
    echo -e "${BLUE}🛠️  自定义配置创建${NC}"
    echo "=================="

    # 收集基本信息
    local display_name provider description
    if ! read -t 30 -p "显示名称: " display_name 2>/dev/null; then
        echo -e "${RED}❌ 输入超时，操作取消${NC}"
        return 1
    fi
    if ! read -t 30 -p "提供商: " provider 2>/dev/null; then
        echo -e "${RED}❌ 输入超时，操作取消${NC}"
        return 1
    fi
    if ! read -t 30 -p "描述: " description 2>/dev/null; then
        echo -e "${RED}❌ 输入超时，操作取消${NC}"
        return 1
    fi

    # 收集API配置
    echo ""
    echo "API配置:"
    local auth_token base_url model timeout
    if ! read -t 30 -p "API密钥 (ANTHROPIC_AUTH_TOKEN): " auth_token 2>/dev/null; then
        echo -e "${RED}❌ 输入超时，操作取消${NC}"
        return 1
    fi
    if ! read -t 30 -p "API端点 (ANTHROPIC_BASE_URL): " base_url 2>/dev/null; then
        echo -e "${RED}❌ 输入超时，操作取消${NC}"
        return 1
    fi
    if ! read -t 30 -p "默认模型 (ANTHROPIC_DEFAULT_SONNET_MODEL): " model 2>/dev/null; then
        echo -e "${RED}❌ 输入超时，操作取消${NC}"
        return 1
    fi
    if ! read -t 30 -p "超时时间 (API_TIMEOUT_MS, 默认3000000): " timeout 2>/dev/null; then
        echo -e "${RED}❌ 输入超时，操作取消${NC}"
        return 1
    fi
    timeout=${timeout:-3000000}

    # 创建配置文件
    cat > "$CONFIG_DIR/$config_name.json" << EOF
{
  "name": "$config_name",
  "display_name": "$display_name",
  "provider": "$provider",
  "description": "$description",
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "$auth_token",
    "ANTHROPIC_BASE_URL": "$base_url",
    "API_TIMEOUT_MS": "$timeout",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "$model",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "$model",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "$model"
  },
  "models": {
    "$model": "默认模型"
  },
  "default_model": "$model",
  "metadata": {
    "created_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
    "updated_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
    "version": "1.0",
    "note": "用户自定义配置"
  }
}
EOF

    echo -e "${GREEN}✅ 自定义配置 '$config_name' 已创建${NC}"
    echo "  配置文件位置: $CONFIG_DIR/$config_name.json"
}

# 交互式菜单
interactive_menu() {
    while true; do
        clear
        echo -e "${BLUE}$(get_text "menu.title")${NC}"
        echo "========================"
        echo ""

        show_status
        echo ""

        echo "$(get_text "menu.operations_title")"

        # 动态显示操作选项
        local resource_file="$I18N_DIR/$CURRENT_LANGUAGE.json"
        if [ -f "$resource_file" ] && command -v jq >/dev/null 2>&1; then
            local operations=$(jq -r '.menu.operations[]' "$resource_file" 2>/dev/null)
            while IFS= read -r operation; do
                if [ -n "$operation" ]; then
                    echo "  $operation"
                fi
            done <<< "$operations"
        else
            # Fallback to hardcoded operations if jq not available
            echo "  1. $(get_text "menu.switch_config" "Switch Configuration")"
            echo "  2. $(get_text "menu.save_config" "Save Current Configuration")"
            echo "  3. $(get_text "menu.list_configs" "List All Configurations")"
            echo "  4. $(get_text "menu.delete_config" "Delete Configuration")"
            echo "  5. $(get_text "menu.backup_config" "Backup Current Configuration")"
            echo "  6. $(get_text "menu.language_switch" "Language Switch")"
            echo "  7. $(get_text "menu.exit" "Exit")"
        fi
        echo ""

        read -p "$(get_text "menu.select_operation")" -n 1 -r
        echo ""

        case $REPLY in
            1)
                echo ""
                echo "$(get_text "menu.input_config")"
                read -p "$(get_text "menu.input_prompt")" config_name
                switch_config "$config_name"
                read -p "$(get_text "menu.press_enter_continue")"
                ;;
            2)
                echo ""
                echo "$(get_text "menu.input_save_name")"
                read -p "$(get_text "menu.input_prompt")" config_name
                save_config "$config_name"
                read -p "$(get_text "menu.press_enter_continue")"
                ;;
            3)
                list_configs
                read -p "$(get_text "menu.press_enter_continue")"
                ;;
            4)
                echo ""
                echo "$(get_text "menu.input_delete_name")"
                read -p "$(get_text "menu.input_prompt")" config_name
                delete_config "$config_name"
                read -p "$(get_text "menu.press_enter_continue")"
                ;;
            5)
                backup_config
                read -p "$(get_text "menu.press_enter_continue")"
                ;;
            6)
                echo ""
                echo "$(get_text "menu.language_title")"

                # 动态显示语言选项
                if [ -f "$resource_file" ] && command -v jq >/dev/null 2>&1; then
                    local language_options=$(jq -r '.menu.language_options[]' "$resource_file" 2>/dev/null)
                    while IFS= read -r option; do
                        if [ -n "$option" ]; then
                            echo "  $option"
                        fi
                    done <<< "$language_options"
                else
                    # Fallback language options
                    echo "  1. Chinese Interface (zh-ui)"
                    echo "  2. English Interface (en-ui)"
                    echo "  3. List Language Configurations"
                    echo "  4. Return to Main Menu"
                fi
                echo ""

                read -p "$(get_text "menu.select_language_option")" -n 1 -r
                echo ""
                case $REPLY in
                    1)
                        switch_language_to "zh-CN"
                        read -p "$(get_text "menu.press_enter_continue")"
                        ;;
                    2)
                        switch_language_to "en-US"
                        read -p "$(get_text "menu.press_enter_continue")"
                        ;;
                    3)
                        echo ""
                        echo -e "${BLUE}$(get_text "language.title")${NC}"
                        echo "===================="
                        for config in "$CONFIG_DIR"/zh-ui.json "$CONFIG_DIR"/en-ui.json; do
                            if [ -f "$config" ]; then
                                local name=$(basename "$config" .json)
                                if command -v jq >/dev/null 2>&1; then
                                    local display_name=$(jq -r '.display_name // "未知"' "$config")
                                    local description=$(jq -r '.description // "无描述"' "$config")
                                    echo -e "  ${GREEN}$name${NC}"
                                    echo "    $display_name"
                                    echo "    $description"
                                    echo ""
                                fi
                            fi
                        done
                        read -p "$(get_text "menu.press_enter_continue")"
                        ;;
                    4)
                        ;;
                    *)
                        echo -e "${RED}$(get_text "menu.invalid_choice")${NC}"
                        read -p "$(get_text "menu.press_enter_continue")"
                        ;;
                esac
                ;;
            7)
                echo "$(get_text "menu.exit")"
                break
                ;;
            *)
                echo -e "${RED}$(get_text "menu.invalid_choice")${NC}"
                read -p "$(get_text "menu.press_enter_continue")"
                ;;
        esac
    done
}

# 主程序
# 预处理命令行参数（处理--lang参数）
preprocess_args() {
    local args=("$@")
    local filtered_args=()
    local i=0

    while [ $i -lt ${#args[@]} ]; do
        local arg="${args[$i]}"
        case "$arg" in
            --lang=*)
                # 语言参数在init_i18n中处理，跳过
                ;;
            -l)
                # 跳过语言参数和下一个参数
                ((i++))
                ;;
            *)
                filtered_args+=("$arg")
                ;;
        esac
        ((i++))
    done

    # 设置处理后的参数数组
    set -- "${filtered_args[@]}"
}

main() {
    # 保存原始参数用于i18n初始化
    local original_args=("$@")

    # 预处理参数
    preprocess_args "$@"

    # Initialize i18n system with original arguments
    init_i18n "${original_args[@]}"

    ensure_dirs
    check_config_file

    case "${1:-}" in
        "list"|"ls"|"l")
            list_configs
            ;;
        "status"|"st"|"s")
            show_status
            ;;
        "save")
            save_config "$2"
            ;;
        "delete")
            delete_config "$2"
            ;;
        "create")
            create_config_interactive
            ;;
        "backup")
            backup_config
            ;;
        "validate"|"val"|"check")
            validate_all_configs
            ;;
        "health"|"hc"|"check-health")
            # 支持参数：--verbose 或 -v
            local verbose="false"
            local config_name="${2:-}"

            # 检查是否为verbose参数
            if [[ "$config_name" == "--verbose" || "$config_name" == "-v" ]]; then
                verbose="true"
                config_name=""
            fi

            health_check "$config_name" "$verbose"
            ;;
        "set-key")
            set_key_config "$2" "$3"
            ;;
        "setup-keys")
            setup_keys_interactive
            ;;
        "router-status"|"router")
            check_router_status
            ;;
        "zh-ui"|"chinese"|"中文")
            switch_language_to "zh-CN"
            ;;
        "en-ui"|"english"|"英文")
            switch_language_to "en-US"
            ;;
        "help"|"-h"|"--help")
            show_help
            ;;
        "")
            interactive_menu
            ;;
        *)
            switch_config "$1"
            ;;
    esac
}

# 运行主程序
main "$@"